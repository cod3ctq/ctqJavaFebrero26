/*
Una interface es un contrato, no es un modelo
No contiene atributos, no constructores, no hay getter y setters
Unicamente existen metodos abstractos
 */
public class IMecanica {
    void cambiarAceite();
    void hacerServicio();
    void manejar();
    void cambiarLlanta();
}
